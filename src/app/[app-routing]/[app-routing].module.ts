import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { [appRouting]import { from } from 'rxjs';
RoutingModule } from './[app-routing]-routing.module';


@NgModule({
  declarations: [],
  imports: [
    CommonModule,
    [appRouting]RoutingModule,
  ]
})
export class [appRouting]Module { }
